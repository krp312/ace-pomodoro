const expect = require('chai').expect;

describe('A passing test just for now', function() {
    it('is something I am trying to get CI to work', function() {
      expect(true).to.be.true;
    });
  }
);
